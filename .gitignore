# OS
.DS_Store
Thumbs.db
# Windows alternate-data-stream metadata created on download; per-machine artefact.
*:Zone.Identifier
# MS Office lock files (created while a .docx/.xlsx/.pptx is open)
~$*

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.venv/
venv/
env/
dist/
build/
*.egg

# uv
.python-version

# Node
node_modules/
.pnpm-store/
.next/
.turbo/
*.log
npm-debug.log*
pnpm-debug.log*

# Build-time artefacts
*.tsbuildinfo
dist-storybook/
coverage/
.vite/

# Go
*.exe
*.test
*.out
coverage.txt
bin/
vendor/

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
ios/Pods/
android/.gradle/
android/local.properties
android/app/build/
ios/build/
build/

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfvars
!example.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Docker
.dockerignore.local

# Secrets + local env
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Build artifacts
*.tar.gz
*.zip

# Helm
# Chart.lock + downloaded deps are regenerated from Chart.yaml; don't check them in
infrastructure/k8s/charts/*/Chart.lock
infrastructure/k8s/charts/*/charts/

# Reports / generated
/reports/
/tmp/

# Local agent state (per-machine, not for the repo)
.claude/

# DB backups (binary dumps + plain SQL — never commit)
backups/
