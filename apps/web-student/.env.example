# Copy to .env.local for local dev.

# Where the API gateway is reachable. Defaults to "/api/v1" (same origin, nginx proxy).
# In pure-frontend dev against a remote gateway, set this to a full URL.
VITE_API_BASE_URL=/api/v1
