name: CI

on:
  pull_request:
    branches: [main, development]
  push:
    branches: [main, development]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Detect which areas of the monorepo changed — skip jobs for untouched stacks.
  changes:
    runs-on: ubuntu-latest
    outputs:
      python: ${{ steps.filter.outputs.python }}
      go: ${{ steps.filter.outputs.go }}
      web: ${{ steps.filter.outputs.web }}
      mobile: ${{ steps.filter.outputs.mobile }}
      infra: ${{ steps.filter.outputs.infra }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            python:
              - 'services/auth/**'
              - 'services/user-profile/**'
              - 'services/content/**'
              - 'services/catalog/**'
              - 'services/search/**'
              - 'services/analytics/**'
              - 'services/payment/**'
              - 'services/institution/**'
              - 'services/notification/**'
              - 'services/adaptive-engine/**'
              - 'ruff.toml'
            go:
              - 'services/quiz/**'
            web:
              - 'apps/web-student/**'
              - 'apps/web-portal/**'
              - 'apps/web-admin/**'
              - 'packages/**'
              - 'pnpm-workspace.yaml'
              - 'package.json'
              - 'pnpm-lock.yaml'
            mobile:
              - 'apps/mobile/**'
            infra:
              - 'infrastructure/**'
              - '.github/workflows/**'

  python:
    needs: changes
    if: needs.changes.outputs.python == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - auth
          - user-profile
          - content
          - catalog
          - search
          - analytics
          - payment
          - institution
          - notification
          - adaptive-engine
    defaults:
      run:
        working-directory: services/${{ matrix.service }}
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
        with:
          version: "0.5.*"
          enable-cache: true
      - run: uv python install 3.11
      - run: uv sync
      - name: Lint (ruff check + format)
        run: |
          uv run ruff check .
          uv run ruff format --check .
      - name: Type check (mypy)
        run: uv run mypy src
        continue-on-error: true  # strict mode; promote to failing once each service's types stabilise
      - name: Test
        run: uv run pytest --tb=short

  go:
    needs: changes
    if: needs.changes.outputs.go == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/quiz
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
          cache-dependency-path: services/quiz/go.sum
      - run: go mod download
      - name: Vet
        run: go vet ./...
      - name: Fmt check
        run: |
          out=$(gofmt -l -s .)
          if [ -n "$out" ]; then echo "gofmt needed:"; echo "$out"; exit 1; fi
      - name: Test
        run: go test -race -count=1 ./...

  web:
    needs: changes
    if: needs.changes.outputs.web == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.12.3
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - name: Build design-system + auth-client + api-client first (apps depend on these)
        run: pnpm -r --filter=@alp/design-system --filter=@alp/auth-client --filter=@alp/api-client build
      - name: Lint (web apps — zero warnings enforced per PR #49)
        run: pnpm -r --filter='./apps/web-*' lint
      - name: Type-check + test + build all @alp/* packages + apps
        run: |
          pnpm -r --filter=@alp/* test
          pnpm -r --filter=@alp/* build

  mobile:
    needs: changes
    if: needs.changes.outputs.mobile == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/mobile
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: "3.24.x"
      - run: flutter pub get
      - run: flutter analyze
      - run: flutter test

  required:
    # Branch protection target — stays green when no relevant stack changed.
    runs-on: ubuntu-latest
    needs: [python, go, web, mobile]
    if: always()
    steps:
      - name: Gate
        run: |
          results='${{ toJson(needs) }}'
          echo "$results"
          for r in $(echo "$results" | jq -r '.[].result'); do
            if [ "$r" != "success" ] && [ "$r" != "skipped" ]; then
              echo "A required job failed."; exit 1
            fi
          done
